# Importing the necessary libraries
library(tidyverse)
message("The libraries have been loaded successfully.")
# Taking a look into the datasets
yearly_deaths <- read_csv("Input Datasets/yearly_deaths_by_clinic.csv")
monthly_deaths <- read_csv("Input Datasets/monthly_deaths.csv")
head(yearly_deaths)
head(monthly_deaths)
View(monthly_deaths)
View(yearly_deaths)
# Adding a proportion column
yearly_deaths <- yearly_deaths %>%
mutate("proportion" = "deaths" / "births")
# Adding a proportion column
yearly_deaths <- yearly_deaths %>%
mutate(proportion = deaths / births)
# Adding a proportion column
yearly_deaths <- yearly_deaths %>%
mutate(proportion = deaths / births)
head(yearly_deaths)
# Adding a proportion column
yearly_deaths <- yearly_deaths %>%
mutate(proportion = round(deaths / births), 2)
head(yearly_deaths)
# Adding a proportion column
yearly_deaths <- yearly_deaths %>%
mutate(proportion = round(deaths / births), 4)
head(yearly_deaths)
# Adding a proportion column
yearly_deaths <- yearly_deaths %>%
mutate(proportion = round(deaths / births, 2))
head(yearly_deaths)
# Adding a proportion column
yearly_deaths <- yearly_deaths %>%
mutate(proportion = round(deaths / births, 4))
head(yearly_deaths)
# Importing the necessary libraries
library(tidyverse)
message("The libraries have been loaded successfully.")
# Taking a look into the datasets
yearly_deaths <- read_csv("Input Datasets/yearly_deaths_by_clinic.csv")
monthly_deaths <- read_csv("Input Datasets/monthly_deaths.csv")
head(yearly_deaths)
head(monthly_deaths)
# Adding a proportion column
yearly_deaths <- yearly_deaths %>%
mutate(proportion = round(deaths / births, 4))
head(yearly_deaths)
# Adding a proportion column
yearly_deaths <- yearly_deaths %>%
mutate(proportion = round(deaths / births, 4))
monthly_deaths <- monthly_deaths %>%
mutate(proportion = round(deaths / births, 4))
head(yearly_deaths)
head(monthly_deaths)
# Adding a proportion column
yearly_deaths <- yearly_deaths %>%
mutate(proportion = round(deaths / births, 4))
monthly_deaths <- monthly_deaths %>%
mutate(proportion = round(deaths / births, 4))
head(yearly_deaths)
head(monthly_deaths)
# Visualising the data
ggplot(data = yearly_deaths,
aes(x = year, y = proportion, colour = clinic)) +
geom_line()
# Visualising the data
ggplot(data = yearly_deaths,
aes(x = year, y = proportion, colour = clinic)) +
geom_line() +
theme_minimal(())
# Visualising the data
ggplot(data = yearly_deaths,
aes(x = year, y = proportion, colour = clinic)) +
geom_line() +
theme_minimal()
# Visualising the yearly deaths
ggplot(data = yearly_deaths,
aes(x = year, y = proportion, colour = clinic)) +
geom_line() +
theme_minimal()
# Visualising the monthly deaths
ggplot(data = monthly_deaths,
aes(x = date, y = proportion)) +
geom_line() +
theme_minimal()
# Visualising the yearly deaths
ggplot(data = yearly_deaths,
aes(x = year, y = proportion, colour = clinic)) +
geom_line() +
theme_minimal()
# Visualising the monthly deaths
ggplot(data = monthly_deaths,
aes(x = date, y = proportion)) +
geom_line() +
theme_minimal()
# Visualising the yearly deaths
ggplot(data = yearly_deaths,
aes(x = year, y = proportion, colour = clinic)) +
geom_line(size = 1) +
theme_minimal()
# Visualising the monthly deaths
ggplot(data = monthly_deaths,
aes(x = date, y = proportion)) +
geom_line(size = 1) +
theme_minimal()
?ifelse
monthly_deaths <- monthly_deaths %>%
mutate(demo = if(test = date > 1847-06-01, yes = TRUE, no = FALSE))
monthly_deaths <- monthly_deaths %>%
mutate(demo = ifelse(test = date > 1847-06-01, yes = TRUE, no = FALSE))
monthly_deaths <- monthly_deaths %>%
mutate(handwashing_started = ifelse(test = date > 1847-06-01, yes = TRUE, no = FALSE))
monthly_deaths
# Adding a proportion column
yearly_deaths <- yearly_deaths %>%
mutate(proportion = round(deaths / births, 4))
monthly_deaths <- monthly_deaths %>%
mutate(proportion = round(deaths / births, 4))
head(yearly_deaths)
head(monthly_deaths)
# Visualising the yearly deaths
ggplot(data = yearly_deaths,
aes(x = year, y = proportion, colour = clinic)) +
geom_line(size = 1) +
theme_minimal()
# Visualising the monthly deaths
ggplot(data = monthly_deaths,
aes(x = date, y = proportion)) +
geom_line(size = 1) +
theme_minimal()
# Adding the information that handwash started from June 01, 1847
monthly_deaths <- monthly_deaths %>%
mutate(handwashing_started = ifelse(test = date > 1847-06-01, yes = TRUE, no = FALSE))
monthly_deaths
# Importing the necessary libraries
library(tidyverse)
message("The libraries have been loaded successfully.")
# Taking a look into the datasets
yearly_deaths <- read_csv("Input Datasets/yearly_deaths_by_clinic.csv")
monthly_deaths <- read_csv("Input Datasets/monthly_deaths.csv")
head(yearly_deaths)
head(monthly_deaths)
# Adding a proportion column
yearly_deaths <- yearly_deaths %>%
mutate(proportion = round(deaths / births, 4))
monthly_deaths <- monthly_deaths %>%
mutate(proportion = round(deaths / births, 4))
head(yearly_deaths)
head(monthly_deaths)
# Visualising the yearly deaths
ggplot(data = yearly_deaths,
aes(x = year, y = proportion, colour = clinic)) +
geom_line(size = 1) +
theme_minimal()
# Visualising the monthly deaths
ggplot(data = monthly_deaths,
aes(x = date, y = proportion)) +
geom_line(size = 1) +
theme_minimal()
# Adding the information that handwash started from June 01, 1847
monthly_deaths <- monthly_deaths %>%
mutate(handwashing_started = ifelse(test = date > 1847-06-01, yes = TRUE, no = FALSE))
monthly_deaths
ggplot(data = monthly_deaths,
aes(x = date, y = proportion, colour = handwashing_started)) +
geom_line(size = 1) +
theme_minimal()
# Adding the information that handwash started from June 01, 1847
monthly_deaths <- monthly_deaths %>%
mutate(handwashing_started = ifelse(test = date > "1847-06-01", yes = TRUE, no = FALSE))
monthly_deaths
ggplot(data = monthly_deaths,
aes(x = date, y = proportion, colour = handwashing_started)) +
geom_line(size = 1) +
theme_minimal()
# Visualising the data again
ggplot(data = monthly_deaths,
aes(x = date, y = proportion, colour = handwashing_started)) +
geom_line(size = 1) +
theme_minimal(size = 1)
# Visualising the data again
ggplot(data = monthly_deaths,
aes(x = date, y = proportion, colour = handwashing_started)) +
geom_line(size = 1) +
theme_minimal()
# Adding the information that handwash started from June 01, 1847
monthly_deaths <- monthly_deaths %>%
mutate(handwashing_started = ifelse(test = date >= "1847-06-01", yes = TRUE, no = FALSE))
monthly_deaths
# Visualising the data again
ggplot(data = monthly_deaths,
aes(x = date, y = proportion, colour = handwashing_started)) +
geom_line(size = 1) +
theme_minimal()
# Importing the necessary libraries
library(tidyverse)
message("The libraries have been loaded successfully.")
# Taking a look into the datasets
yearly_deaths <- read_csv("Input Datasets/yearly_deaths_by_clinic.csv")
monthly_deaths <- read_csv("Input Datasets/monthly_deaths.csv")
head(yearly_deaths)
head(monthly_deaths)
# Adding a proportion column
yearly_deaths <- yearly_deaths %>%
mutate(proportion = round(deaths / births, 4))
monthly_deaths <- monthly_deaths %>%
mutate(proportion = round(deaths / births, 4))
head(yearly_deaths)
head(monthly_deaths)
# Visualising the yearly deaths
ggplot(data = yearly_deaths,
aes(x = year, y = proportion, colour = clinic)) +
geom_line(size = 1) +
theme_minimal()
# Visualising the monthly deaths
ggplot(data = monthly_deaths,
aes(x = date, y = proportion)) +
geom_line(size = 1) +
theme_minimal()
# Adding the information that handwash started from June 01, 1847
monthly_deaths <- monthly_deaths %>%
mutate(handwashing_started = ifelse(test = date >= "1847-06-01", yes = TRUE, no = FALSE))
monthly_deaths
# Visualising the data again
ggplot(data = monthly_deaths,
aes(x = date, y = proportion, colour = handwashing_started)) +
geom_line(size = 1) +
theme_minimal()
?group_by
# Visualising the data again
ggplot(data = monthly_deaths,
aes(x = date, y = proportion, colour = handwashing_started)) +
geom_line(size = 1) +
theme_bw()
# Visualising the data again
ggplot(data = monthly_deaths,
aes(x = date, y = proportion, colour = handwashing_started)) +
geom_line(size = 1) +
theme_simple()
# Visualising the data again
ggplot(data = monthly_deaths,
aes(x = date, y = proportion, colour = handwashing_started)) +
geom_line(size = 1) +
theme_minimal()
# Visualising the data again
ggplot(data = monthly_deaths,
aes(x = date, y = proportion, colour = handwashing_started)) +
geom_line(size = 1) +
theme_classic()
# Visualising the monthly deaths
ggplot(data = monthly_deaths,
aes(x = date, y = proportion)) +
geom_line(size = 1) +
theme_classic()
# Visualising the yearly deaths
ggplot(data = yearly_deaths,
aes(x = year, y = proportion, colour = clinic)) +
geom_line(size = 1) +
theme_classic()
monthly_deaths %>%
group_by(handwashing_started) %>%
summarise(mean(proportion))
monthly_summary <- monthly_deaths %>%
group_by(handwashing_started) %>%
summarise(death_proportion = mean(proportion))
# Generating the summary
monthly_summary <- monthly_deaths %>%
group_by(handwashing_started) %>%
summarise(death_proportion = mean(proportion))
monthly_summary
# Taking a look into the datasets
yearly <- read_csv("Input Datasets/yearly_by_clinic.csv")
# Taking a look into the datasets
yearly <- read_csv("Input Datasets/yearly_deaths_by_clinic.csv")
monthly <- read_csv("Input Datasets/monthly.csv")
# Importing the necessary libraries
library(tidyverse)
message("The libraries have been loaded successfully.")
# Taking a look into the datasets
yearly <- read_csv("Input Datasets/yearly_deaths_by_clinic.csv")
monthly <- read_csv("Input Datasets/monthly_deaths.csv")
head(yearly)
head(monthly)
# Adding a proportion column
yearly <- yearly %>%
mutate(proportion = round(deaths / births, 4))
monthly <- monthly %>%
mutate(proportion = round(deaths / births, 4))
head(yearly)
head(monthly)
# Visualising the yearly deaths
ggplot(data = yearly,
aes(x = year, y = proportion, colour = clinic)) +
geom_line(size = 1) +
theme_classic()
# Visualising the monthly deaths
ggplot(data = monthly,
aes(x = date, y = proportion)) +
geom_line(size = 1) +
theme_classic()
# Adding the information that handwash started from June 01, 1847
monthly <- monthly %>%
mutate(handwashing_started = ifelse(test = date >= "1847-06-01", yes = TRUE, no = FALSE))
monthly
# Visualising the data again
ggplot(data = monthly,
aes(x = date, y = proportion, colour = handwashing_started)) +
geom_line(size = 1) +
theme_classic()
# Generating the summary
monthly_summary <- monthly %>%
group_by(handwashing_started) %>%
summarise(death_proportion = mean(proportion))
monthly_summary
# Imported libraries
library(tidyverse)
# Load and inspect the data
yearly <- read_csv('Input Datasets/yearly_deaths_by_clinic.csv')
yearly
monthly <- read_csv("Input Datasets/monthly_deaths.csv")
monthly
# Add proportion_deaths to both data frames
yearly <- yearly %>%
mutate(proportion_deaths = deaths / births)
monthly <- monthly %>%
mutate(proportion_deaths = deaths / births)
# Plot the data
ggplot(yearly, aes(x = year, y = proportion_deaths, color = clinic)) +
geom_line()
ggplot(monthly, aes(date, proportion_deaths)) +
geom_line() +
labs(x = "Year", y = "Proportion Deaths")
# Add the threshold and flag and plot again
handwashing_start = as.Date('1847-06-01')
monthly <- monthly %>%
mutate(handwashing_started = date >= handwashing_start)
ggplot(monthly, aes(x = date, y = proportion_deaths, color = handwashing_started)) +
geom_line()
# Find the mean
monthly_summary <- monthly %>%
group_by(handwashing_started) %>%
summarize(mean_proportion_deaths = mean(proportion_deaths))
monthly_summary
